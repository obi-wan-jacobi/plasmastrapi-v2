import Component from '../abstracts/Component';
import IRenderingProfile from '../interfaces/IRenderingProfile';

export default class RenderingComponent<TProfile extends IRenderingProfile> extends Component<TProfile> {

}
